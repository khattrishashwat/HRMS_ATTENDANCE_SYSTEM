/** Org master data fetch used after HOST_ADMIN org context switch */
export async function getOrgMasterData(_orgId: number): Promise<void> {
  // Intentionally no-op until org master API is restored under servicesAPI.
}
